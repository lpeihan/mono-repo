# Mono Repo

> 基于 Vue 3 + TypeScript + Vite 的 Monorepo 项目，使用 pnpm + Turbo 进行高效的工程化管理

## 📦 项目结构

```
mono-repo/
├── apps/                    # 应用目录
│   ├── admin/              # 管理后台（Vue 3 + Ant Design Vue）
│   └── h5/                 # H5 移动端应用（Vue 3 + Vant）
├── packages/               # 共享包目录
│   ├── components/         # 共享组件库
│   ├── utils/              # 共享工具函数
│   └── web3/               # Web3 相关功能
└── ...
```

## ✨ 特性

- 🚀 **Monorepo 架构** - 使用 pnpm workspace + Turbo 实现高效的多包管理
- 💪 **TypeScript** - 完整的类型支持
- ⚡️ **Vite** - 极速的开发体验和构建性能
- 🎨 **TailwindCSS** - 实用优先的 CSS 框架
- 📱 **多应用支持** - Admin 管理后台 + H5 移动端
- 🌍 **国际化** - 内置 i18n 多语言支持
- 🎭 **Mock 数据** - 开发环境下支持 Mock 数据
- 🔧 **完整工具链** - ESLint + Prettier + Stylelint + Husky + Commitlint
- 📦 **代码复用** - 共享组件、工具函数和业务逻辑

## 🛠 技术栈

### 核心框架

- **Vue 3** - 渐进式 JavaScript 框架
- **TypeScript** - JavaScript 的超集
- **Vite** - 下一代前端构建工具

### UI 组件库

- **Ant Design Vue** - Admin 管理后台
- **Vant** - H5 移动端组件库

### 状态管理与路由

- **Pinia** - Vue 3 状态管理
- **Vue Router** - 官方路由管理器

### 样式方案

- **TailwindCSS** - 实用优先的 CSS 框架
- **Less** - CSS 预处理器

### 工具链

- **pnpm** - 快速、节省磁盘空间的包管理器
- **Turbo** - 高性能构建系统
- **ESLint** - 代码质量检查
- **Prettier** - 代码格式化
- **Stylelint** - 样式代码检查
- **Husky** - Git 钩子工具
- **Commitlint** - Commit 信息规范检查

### 其他工具

- **Axios** - HTTP 客户端
- **Day.js** - 日期处理库
- **Vue i18n** - 国际化方案
- **NProgress** - 进度条
- **Store2** - 本地存储封装

## 📋 前置要求

- **Node.js** >= 18.0.0
- **pnpm** >= 9.1.0

## 🚀 快速开始

### 安装依赖

```bash
pnpm install
```

### 开发模式

```bash
# 启动所有应用
pnpm dev

# 单独启动 Admin
pnpm --filter admin dev

# 单独启动 H5
pnpm --filter h5 dev
```

### 构建

```bash
# 构建所有应用（测试环境）
pnpm build

# 构建所有应用（生产环境）
pnpm build:production

# 单独构建 Admin
pnpm --filter admin build

# 单独构建 H5
pnpm --filter h5 build
```

### 预览

```bash
# 预览所有应用
pnpm preview

# 单独预览 Admin
pnpm --filter admin preview

# 单独预览 H5
pnpm --filter h5 preview
```

## 📦 部署

```bash
# 部署 Admin（测试环境）
pnpm deploy:admin

# 部署 Admin（生产环境）
pnpm deploy:admin:production

# 部署 H5（测试环境）
pnpm deploy:h5

# 部署 H5（生产环境）
pnpm deploy:h5:production
```

## 🔍 代码质量

### Lint 检查

```bash
# ESLint 检查
pnpm lint

# ESLint 自动修复
pnpm lint:fix

# Stylelint 检查并修复
pnpm stylelint

# 拼写检查
pnpm cspell
```

### 代码格式化

```bash
pnpm prettier
```

## 📝 提交规范

本项目使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范，提交信息格式如下：

```
<type>(<scope>): <subject>
```

### Type 类型

- `feat`: 新功能
- `fix`: 修复 Bug
- `docs`: 文档变更
- `style`: 代码格式调整（不影响代码运行）
- `refactor`: 重构（既不是新增功能，也不是修复 Bug）
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动
- `ci`: CI 配置文件和脚本的变动
- `build`: 影响构建系统或外部依赖的变更

### 示例

```bash
git commit -m "feat(admin): 添加用户管理功能"
git commit -m "fix(h5): 修复列表页滚动加载问题"
git commit -m "docs: 更新 README 文档"
```

## 📂 应用介绍

### Admin 管理后台

- **技术栈**: Vue 3 + Ant Design Vue + TypeScript
- **功能**:
  - 用户登录认证
  - 用户管理
  - 个人资料管理
  - 主题切换（明暗模式）
  - 多语言切换
  - 响应式布局

### H5 移动端

- **技术栈**: Vue 3 + Vant + TypeScript
- **功能**:
  - 用户登录认证
  - 首页导航
  - 列表与详情页面
  - 无限滚动加载
  - 移动端适配
  - 多语言切换
  - Web3 集成

## 📚 共享包说明

### @packages/components

共享的 UI 组件库，包含：

- `SvgIcon` - SVG 图标组件

### @packages/utils

共享的工具函数库，包含：

- 剪贴板操作
- 样式类名合并工具
- 其他通用工具函数

### @packages/web3

Web3 相关功能封装，包含：

- 智能合约交互
- USDT ABI 配置
- Web3 工具函数

## 🔧 开发配置

### VS Code 推荐插件

- Vue - Official
- ESLint
- Prettier - Code formatter
- Stylelint
- Tailwind CSS IntelliSense

### EditorConfig

项目已配置 `.editorconfig` 确保团队成员使用一致的编辑器配置。

## 📄 许可证

ISC

## 👥 作者

请在 `package.json` 中添加作者信息

---

如有问题或建议，欢迎提交 Issue 或 Pull Request！
